name : surya-cicd

on: push  

jobs: 
  surya-cicd: 
    name: surya code base
    runs-on: ubuntu-latest 
    steps: 
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run surya-cicd
        uses: github/surya-cicd@v3
        env: 
           DEFAULT_BRANCH: main 
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  
